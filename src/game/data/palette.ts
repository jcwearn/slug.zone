// Shared with the landing page. #54e508 is the lime the site has used since
// 2019 and is the one colour that must not drift -- the border, the type, and
// the game's fog and HUD all key off it.
export const LIME = 0x54e508

// The slug browns, taken from public/slug.png so enemies read as the same
// creature the front page shows.
export const SLUG_BROWN = 0x8b6b3f
export const SLUG_DARK = 0x4a3520
export const SLUG_PALE = 0xc4a878

// Level surfaces. Deliberately desaturated so the lime stays the brightest
// thing on screen and the eye tracks it.
export const WALL_BRICK = 0x5a4a3a
export const WALL_METAL = 0x6b6b73
export const FLOOR_DAMP = 0x3a3a30
export const CEIL_CONCRETE = 0x2a2a2a
